# Claim Ledger — Whoosh'd Queue, Batching, and Docs Closure

Which claims are safe, qualified, experimental, research-only, and
explicitly forbidden.

## Safe Current Claims

- Whoosh'd has a navigable documentation portal
- Whoosh'd documents queue/admission, scheduler, ThreadWake, guarded batching, and runtime validation
- Whoosh'd records the Cave Thunder decision
- Whoosh'd records the batching arc closeout
- Whoosh'd records the documentation pass closeout

## Safe Qualified Claims

- Guarded adapter batching exists and has scoped validation
- HTTP queue/admission grouping validation passed under explicit guarded test conditions
- Fake backend token-step contracts prove scheduler shape in a sandbox
- Runtime validation packets provide scoped evidence under recorded conditions

## Experimental Claims

- Guarded adapter batching is experimental and disabled by default
- ThreadWake behavior depends on backend support and validation
- Cache-aware scheduling posture remains claim-bounded

## Research-Only Claims

- Token-step shared decode scheduling remains research-only for MLX
- Whoosh'd-owned MLX decode-loop scheduling remains blocked
- Durable ThreadWake snapshots remain deferred

## Do-Not-Use Claims

- Whoosh'd has production-ready batching
- Whoosh'd improves latency
- Whoosh'd improves throughput
- Whoosh'd implements true continuous batching
- MLX token-step scheduling is implemented
- ThreadWake is AI memory
- ThreadWake remembers conversations
- Fake backend proof proves MLX capability
- Runtime validation certifies production readiness

## Open Evidence Questions

- What benchmark packet would be required before latency/throughput claims?
- What backend primitive evidence would reopen MLX token-step scheduling?
- What validation packet would be required for additional backend batching claims?
- What security design would be required before durable ThreadWake snapshots?
