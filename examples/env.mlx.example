# Whoosh'd — MLX in-process adapter example env
# Requires mlx-lm installed: pip install mlx-lm
# Copy to .env and run:
#   WHOOSHD_ADAPTER=mlx uvicorn whooshd.app:app --port 8000

WHOOSHD_ADAPTER=mlx
WHOOSHD_MLX_MODEL=mlx-community/Llama-3.2-3B-Instruct-4bit
